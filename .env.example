# SpekGen Ops — credenciales locales
# COPIAR como .env y llenar — NUNCA commitear .env

# Google Drive API — OAuth access token (expira cada hora, usar service account para producción)
GOOGLE_ACCESS_TOKEN=ya29.xxx

# ClickUp personal API token
CLICKUP_TOKEN=pk_xxx

# Alternativa: service account JSON (recomendado para automatización)
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
